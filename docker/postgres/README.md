# PostgreSQL

The local PostgreSQL service is defined in `docker-compose.yml`.

Phase 0 intentionally does not create business tables. Schema changes should be added later through migrations in `apps/backend/src/db/migrations`.
